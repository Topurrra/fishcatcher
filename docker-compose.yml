services:
  fishcatcher-site:
    build: .
    container_name: fishcatcher-site
    restart: unless-stopped
    ports:
      - "127.0.0.1:8419:8080"
    networks:
      - fishcatcher

networks:
  fishcatcher:
    name: fishcatcher
