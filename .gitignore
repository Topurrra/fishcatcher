dist/
node_modules/
*.log
.DS_Store
Thumbs.db
scripts/data/
docs/
.gstack/

# voli project memory (local knowledge, not for the repo)
.voli/

# local scanner notes
omnis-x-fp.md
store/

# the threat registry is its own repo (Topurrra/fishcatcher-registry); a local clone may live here
registry/
